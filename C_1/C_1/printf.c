#include <stdio.h>    //printf 등 기존 라이브러리 함수들이 선언 된 stdio헤더 파일 포함
int main() { // 메인함수는 말그대로 프로그램의 메인이 되는 함수, 하나만 존재, 기본적인 C언어 프로그램 틀

	printf("안녕하세요.저는 김브래 입니다!\n잘 부탁 드립니다!");

	//printf는 화면에 문자나 기호를 출력하는 함수이다.
	//사용법은 printf("~~"); 이다.세미콜론은 모든 문장 뒤에 붙인다.
	// \n은 한 줄 띄는 명령어 이다.
	return 0;
}